import * as React from 'react';
import TextField from '@mui/material/TextField';

export default function InputOutline(props) {
    return (
        <TextField id="outlined-basic" label="Outlined" variant="outlined" />
    );
}